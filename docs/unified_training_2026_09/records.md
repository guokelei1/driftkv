# 本轮统一训练记录

## 2026-09-15：固定 Medium V0–V5

用户确认本轮完整链，并要求只保留最终模型与对应记录，清除被替代任务和失败记录。

- V0/V1：复用历史seed17共享V0、D14 V1。
- V2：从V1训练2 epochs，全局batch32、4卡。
- V3/V4/V5：分别从直接parent训练1 epoch，其他配置一致。
- 仅E14，四条新训练边相对AUC分别提升1.138%、2.369%、3.095%、3.283%，全部通过原准入指标。
- 全部checkpoint已集中于`results/unified_training_2026_09/medium/seed17/checkpoints/`；哈希与原seal一致，逐条验证直接parent哈希连续。旧运行路径通过符号链接保留。
- 本轮被替代的V2任务、失败记录及短检查payload已删除；最终训练/评测证据、合同、配置和轻量启动证明保留。被删除产物不再可用；这是开发选择后的最终链，不是无选择的独立评价。

完整来源、指标和路径统一维护在[模型链索引](../../results/unified_training_2026_09/medium/README.md)。三档规模仍见[计划](plan.md)。

## 2026-09-15：固定 Large V0–V3

- 用户要求先固定历史10L的V0–V3，V4/V5随后重训。选择历史D14、seed17链前四个模型，均为1 epoch；模型为10L/H320、10 heads、context1024、79,681用户。
- 四份checkpoint及原seal已复制至`results/unified_training_2026_09/large/seed17/checkpoints/v0..v3/`，约10.69 GiB。复制前后核对权重/封存哈希，并逐条验证payload内的直接parent哈希和训练窗口；历史原件保留原位。
- 历史完整E14的V0→V1、V1→V2、V2→V3相对AUC提升分别为3.396%、2.072%、2.750%。此次只复用模型，没有重新评测；精确来源和报告哈希已登记。
- [Large固定链索引](../../results/unified_training_2026_09/large/README.md)及机器可读清单为后续入口。当前固定链止于V3，旧V4/V5不纳入本轮；本次未启动重训或修改历史模型。

## 2026-09-15：启动 Large V4，global batch64 / 2 epochs

- 用户明确授权从固定V3训练V4，全局batch64，四卡每卡16，2 epochs；仅保存最终2 epochs checkpoint，带E14，后台启动后结束交互。CPU14、LR=5e-5、weight decay=1e-4、seed17及用户等权BCE保持。
- 训练[259,273)，评价[273,287)；两epoch连续AdamW，每轮重新按相同rank内时间顺序遍历，沿用旧Large staged训练顺序。合同只列checkpoint_epochs=[2]，不保存半轮/一轮端点。
- 8步训练与188行父子原始打分检查通过；父模型哈希、配置、有限参数/分数、四卡拓扑、显存余量和只保存最终端点的步数映射均核对。探针权重已删除。
- 345,226条请求，2 epochs为10,790步；探针121.85请求/秒，峰值reserved 23,780 MiB。纯训练估计94.5分钟，含加载保存和E14预算125–155分钟。
- 合同`configs/contracts/yambda_large_v4_2epoch_4gpu_b64_20260915.yaml`，执行配置`configs/unified_training_2026_09/large_v4_4gpu_b64_cpu14_execution.yaml`；后台会话`large_v4_b64_20260915`。[运行目录](../../results/unified_training_2026_09/large/seed17/v4_2epoch_4gpu_b64_cpu14/README.md)保存日志、退出码、配置和结果。不自动重试、扩展epoch或启动V5。

## Large V4 batch64：完成结果

- 正式训练5,995.40秒（99.9分钟），E14打分2,334.19秒、汇总26.28秒，总计约139.5分钟；退出码0。
- E14 [273,287)共358,006条请求、37,111用户。Parent V3 AUC=0.68058719，新V4 AUC=0.68992200，相对提升1.371582%；原四项准入指标及>1%研究目标全部通过。历史batch96、2 epochs的对应提升为0.855419%。
- Log loss从0.31639262降至0.31267229（下降1.175857%），Brier从0.08989250降至0.08873745。辅助dislike-only log loss从2.06824647升至2.14664233，并非所有指标一致改善。
- 仅保留`checkpoint/checkpoint_epoch_2.pt`；权重、原始分数、adjudication哈希与seal一致。结果属于看过历史质量后的seed17开发实验，尚未启动V5或执行Reuse。

## Large V5：启动连续2 epochs，保存并评测1/2两个端点

- 用户明确要求继续V5，分别保留1 epoch和2 epochs checkpoint并都评测。Parent为本轮达标V4，SHA-256为`8df05079a7b8c42a3e71d7b173e2d2dfe6ba24852abf31a18556b1ab55b912a5`；父模型和准入报告哈希已核对。
- 四卡每卡16/global64、CPU14、seed17、fresh AdamW、LR=5e-5，2 epochs内不重置优化器。训练[273,287)，同窗评测E14 [287,301)。沿用历史day300不完整标记，不将其作为完整E14证据。
- checkpoint_epochs=[1,2]；358,006条请求，两轮11,188步，分别在5,594和11,188步保存。训练完成后联合打分V4/V5_e1/V5_e2，分别报告原四项指标与相对AUC >1%目标，不自动选择端点。
- 8步训练及939行三模型raw检查通过；同一探针权重的双候选打分逐位一致，验证候选汇总完整性。两个已有epoch计划测试通过。探针权重已删除。
- 探针114.63请求/秒，峰值reserved23,852 MiB；纯训练估计104.1分钟。参考上一轮双模型E14的39分钟，三模型约60分钟，含加载保存预算160–195分钟。
- 合同`configs/contracts/yambda_large_v5_epochs12_4gpu_b64_20260915.yaml`，执行配置`configs/unified_training_2026_09/large_v5_4gpu_b64_cpu14_execution.yaml`；后台会话`large_v5_b64_20260915`。[运行目录](../../results/unified_training_2026_09/large/seed17/v5_epochs12_4gpu_b64_cpu14/README.md)保留日志、退出码和自动结果。

## Large V5：两个端点评测完成

- 训练6,208.72秒（103.5分钟），三模型E14打分3,469.67秒、汇总38.31秒，总计约162分钟，退出码0。两份checkpoint、raw和报告哈希均与seal一致。
- E14 [287,301)共370,165条请求、38,312用户，day300不完整，继续标记为方向性开发结果。Parent V4 AUC=0.67462011。
- V5@1：AUC=0.68391930，相对+1.378434%；loss=0.29064362，Brier=0.08059950。达到AUC >1%目标，但用户级loss bootstrap下界不为正，原四项准入未全通过。
- V5@2：AUC=0.71666377，相对+6.232197%；loss=0.28045024，Brier=0.07772462。达到AUC >1%目标且原四项准入均通过。两个端点的dislike-only loss均比parent更高，辅助指标未全部改善。
- 两份checkpoint均保留，未自动选择端点、推广谱系或执行Reuse。

## Large V0–V5：按用户选择暂定 V5@1 并集中保存

- 用户明确选择先暂定V5的1 epoch端点，要求整理完整10L版本链。工作链为历史V0–V3、本轮V4@2、V5@1；V5@2继续作为备选保留。
- V4/V5实体权重集中至`large/seed17/checkpoints/v4/`和`v5/`，统一名`checkpoint_100.pt`；原训练路径保留相对符号链接，不改变已封存路径或内容。
- 当前完整清单为`large/seed17/checkpoints/chain.v0_v5.manifest.json`。原`chain.manifest.json`仅为V0–V3前缀，被训练合同哈希引用，保持原样。
- V5@1的AUC相对提升1.378434%，但原四项准入未全通过；用户级loss bootstrap失败标记及day300不完整范围保持。此次是用户暂定选择，不改写准入结果，不等同服务推广。
- [Large链文档](../../results/unified_training_2026_09/large/README.md)统一记录训练窗口、epoch、batch、父子关系和五条E14收益。没有新增训练或删除备选模型。

## 2026-09-15：Max 20万用户准备完成

用户授权5B人口处理与资源探针，尚未授权正式Max长训练。按历史label-free资格与SHA256排序，从796,134名合格用户选择200,000人，未要求包含Large。完整保留1,127,650,996条listen和19,843,071条反馈；day217前词表3,530,650项，V0有效监督13,250,545条。原始SHA256、完整人口/映射/请求因果性审计及8个实际历史参考对照通过。

Max显式使用known=1..K、OOV=K+1..K+256、PAD=0，并保留原始item的同时间戳顺序；旧数据默认行为不变。保留16L/H320/10heads/context1024，总参数1,138,203,521。四卡batch64/80/96满长度探针后选global80（每卡20），吞吐86.29请求/秒，峰值reserved36,672 MiB，余量19.4%；96余量不足15%，未运行128。评测流程小样本通过，未计算质量指标。

V0准备1 epoch，纯训练约42.7小时，暂排43–52小时；后续窗口约3小时/epoch、评测另计。固定词表后期未知目标比例最高约17.3%，排除口径及day300不完整均保留记录。一次性探针权重清理，日志/摘要/评测封存保留。[详细入口](../../results/unified_training_2026_09/max/README.md)。V0合同与执行脚本已准备，显式启动标志保持false。

## 2026-09-15：Max 长训练恢复点

用户要求减少40余小时训练中断后的损失。新增同布局四卡恢复：第500步首次保存，之后每4000步保存，保留最近两份完整恢复点。保存模型本地分片、AdamW状态、随机状态及已完成步数；临时文件写入并校验后发布完整标记，旧点在新点完整后才淘汰。启动脚本支持`--resume`，最终权重封存后清理恢复权重。

完整Max恢复点约13.66 GB，写盘及校验约17秒，按4000步间隔约0.5%开销，V0累计约12分钟。合成数据对照有一个参数1.86e-9、两项优化器元素最大4.77e-8差异，严格断言失败记录保留。实际训练脚本的真实数据对照，从第8步恢复至第12步，与连续12步最终模型逐位一致。恢复路径增加一次未使用显存缓存释放；最终资源及一致性记录见Max结果入口。正式长训练仍未启动。

## 2026-09-15 23:33：Max V0正式启动

用户要求再次全面检查，通过后启动基础V0。88个处理文件的哈希、全部冻结输入、代码和证据绑定核对通过；复用已通过的实际训练/恢复及资源canary。四张A40空闲，磁盘约724 GiB可用。V0为20万用户、13,250,545条known监督、16L/H320/10heads/context1024、seed17、1 epoch、global80，每rank CPU14。第500步首个恢复点，此后每4000步，保留两份。

已记录本次条件启动授权、冻结正式合同/执行哈希，在`evokv_max_v0_20260915` detached tmux启动；预计43–52小时，实际进度会更新。用户期望基础AUC约0.6–0.7、后续相对提升约1%–4%，这些是质量期望，不能预先保证；本次仅启动V0，不自动训练后续版本或运行Reuse。当前只确认启动，未产生最终质量结论。

## 2026-09-18：Max V0 10%用户E14抽样AUC

用户授权先抽10%用户评测，完成后再决定更大范围。与标签无关的独立SHA256顺序固定20,000/200,000 UID，不补选无有效反馈用户。V0训练[0,217)，评价[217,231)，Full/context1024/known target口径不变。有效9,114人、96,750请求（88,538 like、8,212 dislike），排除4,795未知目标请求。结果ROC-AUC 0.6535090485、log loss 0.2818309273。原始分数先封存再关联标签，复用现有指标实现。数值/请求完整性检查通过，四卡总耗时约10分17秒，退出码0。

[抽样结果及绑定](../../results/unified_training_2026_09/max/seed17/v0_e14_users10pct/summary.json)。该结果符合用户基础质量期望；未开展全人口评测、V1训练或父子版本准入。单模型与显式UID名单支持加入现有Full evaluator，数值评分逻辑保持不变，新的配置记录对应源代码哈希；历史训练封存不改写。
