# Tests

The former test suite was coupled to retired experiment runners, configs, checkpoints and generated results, so it was removed together with those artifacts. New tests should be added only after the 37D core contract is frozen.
