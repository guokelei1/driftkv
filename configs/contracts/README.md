# Contract registry boundary

Contracts in this directory are immutable evidence or prospective records; a
filename does not authorize execution.

- Historical numbered P7--P11, CC and 8L contracts were removed together with
  their old execution paths. They are not supported evidence inputs for the
  current motivation.
- `yambda500m_scale_population_v1.yaml` freezes the active label-free Medium/
  Large UID selection and foundation-only compact mapping boundary; it does not
  authorize training or theta3 access.
- `yambda500m_streaming_windows_v1.yaml` defines composable daily slices and
  calendar-only release capacity. Locked theta3/later slots are not executable
  qualification contracts.
- `yambda500m_unified_scales_v1.yaml` defines the current one-pass shared
  preprocessing route for nested Yambda-500M S/M/L logical datasets.
- `yambda500m_large_hstu_native_d7_d14_full_reuse_pro_v1.yaml` prospectively
  freezes the user-approved 79,681-user Large point at 10L/H320/10 heads/C1024,
  ten D7 E7 edges and five D14 E7/E14 edges. The fifth nominal E14 is explicitly
  `E14_partial`. It fixes C64 PRO at 8.919% of Exact theoretical FLOPs and
  prohibits quality-driven architecture/runtime/estimator selection.
- `yambda500m_large_hstu_native_d7_d14_execution_v1.yaml` binds that base
  contract to the sealed Large request manifest and passing label-free four-A40
  resource canary. It freezes global train batch96, Full batch64/rank and
  Reuse/PRO cohort12/query128 before formal Large quality is read.
- `yambda500m_large_reuse_scope_d14_e14_only_v1.yaml` transparently records the
  user-authorized 2026-08-31 post-commencement scope reduction: training and all
  20 Full/admission cells remain unchanged, while formal Reuse+C64-PRO runs only
  the five D14/E14 cells. D7/E7 and D14/E7 Reuse are explicitly excluded; the
  fifth cell remains `E14_partial`.
- `yambda500m_large_full_only_stop_v1.yaml` supersedes that execution scope
  before any formal Reuse cell was launched. It preserves all checkpoints,
  Full-only cells and admission seals, sets the formal Reuse/PRO cell count to
  zero, and ends the Large queue after Full-only completion.
- `yambda500m_large_v3_v4_epoch_sweep_v1.yaml` freezes the post-hoc D14
  v3→v4 endpoint-strength diagnostic after its one-epoch E14 failure was
  observed. One continuous trajectory saves 0.5/1.0/1.5/2.0-epoch checkpoints
  and reports all four together on E14 Full-only. It is development evidence,
  prohibits Reuse/PRO and requires a focused canary plus a separate explicit
  formal launch.
- `yambda500m_medium_hstu_native_d7_d14_full_reuse_v1.yaml` freezes the Medium
  day217 D7/D14 preparation, original four-rank plan, direct-parent training windows,
  and three-path Parent/Current/adjacent-Reuse metrics. CPU preparation and a
  focused canary are authorized; its physical execution fields are superseded
  prospectively by the GPU2/3 supplement below. The formal flag must be invoked
  explicitly by the user, and no PRO or serving-lineage promotion is authorized.
- `yambda500m_medium_hstu_native_d7_d14_execution_admission_v1.yaml` prospectively
  amends that frozen matrix to physical GPU2/3 only, two FSDP ranks and global
  batch 32 (16/16). It also requires Full-only release-eligibility sealing before
  adjacent Reuse and keeps rejected candidates out of the accepted diagnostic
  lineage. It does not authorize formal long training, PRO or serving promotion.
- `yambda500m_medium_hstu_native_d14_cpu_parallelism_canary_v1.yaml` records the
  abandoned full-scan CPU timing-canary plan. It produced no selection artifact;
  the run was stopped when its cost was found to be comparable to formal work.
- `yambda500m_medium_hstu_native_d14_cpu_runtime_v2.yaml` freezes the resumed D14
  evaluator runtime at 14 disjoint NUMA-local physical cores per GPU rank (28
  total) after D7 completion. Its 16-user raw-only correctness canary passed; it
  changes no data, checkpoint, batch, metric, admission, or D7 artifact.
- `yambda500m_medium_hstu_native_d14_reuse_4gpu_runtime_v3.yaml` prospectively
  moves only the unfinished D14 adjacent-Reuse cells to physical GPU0/1/2/3,
  four evaluator ranks, cohort 32 and query chunk 256. It preserves completed
  two-rank results and requires a 64-user/rank raw-only canary before resume.
- `yambda500m_medium_hstu_native_d7_forced_reuse_diagnostic_v1.yaml` freezes the
  later user-authorized completion of all twenty D7 E3/E7 adjacent-Reuse cells.
  It preserves hashes of all rejected D7 admission seals and candidate-chain
  checkpoints, reuses the proven four-GPU cohort32/query256 runtime, and writes
  to a separate diagnostic-only result family. It cannot unlock admission,
  alter formal lineage/summary, or support a qualification claim.
- `yambda500m_medium_hstu_native_d14_v5_extension_v1.yaml` adds exactly one
  direct-parent Medium D14 candidate, v4→v5. Training `[273,287)` and E3/E7 are
  complete-data evidence; nominal E14 `[287,301)` includes incomplete source
  day300 and is sealed/reported only as `E14_partial`, never as qualification.
- `yambda500m_medium_hstu_native_d14_v5_execution_v1.yaml` binds that extension
  to one serial GPU0/1/2/3 four-rank queue, global train batch 32 (8/rank), Full
  batch 128/rank and the proven Reuse cohort32/query256 runtime. It requires a
  raw-only longest-window canary before the full queue.
- `yambda500m_small_foundation_chain_v1.yaml` freezes Small foundation
  data/model/cache/metric semantics. It authorizes implementation and correctness
  canaries, not real Base fitting or HSTU training.
- `yambda500m_small_seed17_launch_v1.yaml` is the user-authorized execution
  record for the four-rank Small seed17 `v0 -> v1` chain, with an optional
  same-recipe `v2` extension. It does not unlock v3/theta3 or M/L.
- `yambda500m_small_hstu_native_recommendation_state_structure_v1.yaml` freezes
  the label-free 3,000-user v0..v5 recommendation-state observation: UID split,
  cutover histories, state-delta stages, matched coreset budget and candidate
  bank. It authorizes focused inference only, not training, candidate admission,
  a new action, or lineage promotion.
- `yambda500m_small_hstu_native_candidate_shared_causal_v1.yaml` freezes the
  signed, head-wise, multi-width controlled and real-exposed candidate causal
  intervention. Its shared/residual paths are diagnostic oracles, not actions.
- `yambda500m_small_hstu_native_evidence_measure_basis_v1.yaml` freezes one
  matched-cost candidate-independent basis canary. That canary failed 0/5 and
  therefore does not authorize formal quality evaluation or mechanism admission.
- `yambda500m_small_hstu_native_reader_compatibility_correction_v1.yaml` corrects
  the claim from a materializable history basis to a candidate-shared reader
  compatibility correction. It freezes stage localization and cross-request
  persistence observations; the single layerwise broadcast-residual mechanism
  remains locked unless both gates pass.
- `yambda500m_small_hstu_native_av_broadcast_residual_v1.yaml` freezes the only
  mechanism unlocked by the passed reader-stage and persistence gates: a
  32-carrier compact Current probe that emits a per-user AV sidecar. Its
  label-free score canary passed 4/5, but formal quality and action admission
  remain prohibited pending expert review.
- `yambda500m_small_hstu_native_pro_lazy_reader_v1.yaml` freezes the first
  no-materialized-prefix PRO correctness/cost audit. Its dimensionful AV
  absolute-error gate failed and remains preserved; cost and structure passed.
- `yambda500m_small_hstu_native_pro_lazy_reader_v2.yaml` changes no mechanism
  setting, uses the next 32 users, and corrects only the FP32 equivalence metric
  to relative AV L2 plus absolute final replay. It passed correctness, zero-state-
  materialization and theoretical-cost gates.
- `yambda500m_small_hstu_native_pro_lazy_rolling_quality_v1.yaml` freezes the
  unchanged 32-carrier lightweight PRO on all five E14 populations. Raw scores
  were sealed before label join. AUC improved on 5/5 edges and log-loss on 3/5;
  mean edge deltas improved both. Overall design viability is positive, while
  the prospective 4/5+4/5 strict gate remains failed. It does not admit a
  serving lineage, runtime qualification, or new training.
- `yambda500m_small_hstu_native_progressive_pro_decomposition_v1.yaml` freezes
  the label-free five-edge error decomposition requested after that viability
  result: exact shared-AV direction/amplitude, cutover-versus-rolling drift,
  two fixed probes and old/recent segment scalar decay. The same five edges are
  development evidence only; their labels cannot qualify the upgraded PRO.
- `yambda500m_small_hstu_native_progressive_pro_frontier_v1.yaml` freezes the
  held-out label-free C32/C48/C64 fidelity frontier after decomposition retained
  one component and global decay. It can select an under-20%-of-Full fidelity
  primary, but cannot read the old five-edge labels or admit an action.
- New release-chain contracts must separate candidate training, Full-only
  admission and accepted-release sealing. Admission may not read Reuse or any
  cache-compatibility artifact; rejected candidates do not enter lineage.
- New active contracts must use the EvoKV-HSTU-S/M/L naming and follow
  `docs/experimental_design.md`.

Do not edit old hashes to make them appear current. Retirement and replacement
are recorded in route documentation and new contracts.
