{
  "scope": "Additional paired response-timing control before4082 cohort results analysis; no new parameter values",
  "rule": "Immediate-return bounded control chooses target score only on an actual installation; uses the two scores already computed in bounded replay",
  "reason": "Separate the effects of delayed response switching and capped observation, including their interaction",
  "validation": "Same transformation of post-unlimited must reproduce every old immediate-witness response bitwise, with identical actions",
  "causal_scope": "Fixed recorded events and requests never depend on returned scores in this offline replay. All candidate computations and installs actually executed. No labels, future actions, weighted output mixture or replacement of state trajectories",
  "cost": "Same executed model work and state actions; return-branch control scalar allowance conservatively identical"
}
