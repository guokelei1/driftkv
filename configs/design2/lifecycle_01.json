{
  "status": "fixed_before_lifecycle_outputs",
  "question": "Does release-only detection followed by real KV rebuild improve later actual feedback quality at lower lifecycle incremental FLOPs than four release Exact-All?",
  "uids": "original detection_01 development1024, same order, no exclusion",
  "models": "frozen six-layer Medium seed17 M0 through M5; M5 E14_partial",
  "targets": [1, 3, 4, 5],
  "cutover_days": [231, 245, 259, 273, 287],
  "stop_day": 301,
  "methods": ["reuse", "design1", "exact", "design2"],
  "initial_condition": "one M0 legal retained prefix per UID at first release; independent caches thereafter; no per-edge Parent reinitialization",
  "policy": "formula reject if b>tau; formula accept if a=0 and b<=tau; otherwise compare remaining full check (prepare view+16 C reads+225D detector) with Current KV rebuild+new summary; rebuild if check>=rebuild, else run frozen detector and rebuild on rejection",
  "threshold": 0.04309522041252682,
  "numerical_policy": "same FP64 bounds and 1e-10 decision guard as conditional225_01; uncertainty escalates to original solve, unless its remaining cost is already >= rebuild",
  "decision_panel": "same lawful held-bank16 items at release time, used only for decisions",
  "service": "real frozen feedback requests; query before writes at same timestamp; native append chunks <=128 cut at every request group; independent four-path replay",
  "rebuild_semantics": "Current full retained prefix replaces real KV and summary; invalidate view, detector and decision; no correction until next model change; preserve release age separately from rebuild anchor",
  "M2": "native transition only, no detector or rebuild, invalidate current-model anchor and old views",
  "quality": "per-edge AUC and equal-four-edge mean; paired UID bootstrap1000; later real requests and writes-since-release bands; no decision-panel quality success criterion",
  "cost": "raw actual calls and lengths; FMA=2; special functions separated and main convention=1; common M0/native writes/plain reads cancel by matched shape; charge original C/PCA dependency, residual calibration sources+Current teachers, H acquisition/Gram/factor, inverse prep once, all state summaries/views, decision reads/detection/rebuilds, later actual correction",
  "extrapolation_users": [30000, 100000, 1000000],
  "canary": "first8 developmentUID; real forced-rebuild reference, summary/eviction, next-release invalidation, zero query mutation; then original1024",
  "alternatives_if_needed": "At most two evidence-driven execution/policy alternatives, frozen before new results; no change to detector. Preserve the initial failure and do not tune against confirmation.",
  "authorization": "2026-09-09 user requests closed loop and permits justified alternatives; no training, no confirmation/theta3 access"
}
