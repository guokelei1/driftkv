{
  "purpose": "Complete the fallback benchmark with FreshCurrent at EVERY actual feedback request in the already frozen workload",
  "population": "same2048 natural, same first512 cap32/128; targets1/3/4/5; no newUIDs, confirmation or training",
  "selection": "all request groups and all candidate rows, without error, score, action or label-based filtering",
  "frozen_method_candidates": "excursion, excursionfull, excursionbg; prior first-use variants retained. No further grouping/threshold/commit-tolerance/cadence tuning using these expanded references",
  "metrics": "keep original checkpoint results separately; add full-coverage UID/window max and UID-equal request burden at0.1/0.5/1.0; no favorable endpoint substitution",
  "cost": "evaluation-only target teacher, separately charged from serving/calibration. Compact cases persisted; history arrays materialized only while evaluating",
  "scope": "expanded development evidence, not independent confirmation; one frozen6-layer model seed"
}
