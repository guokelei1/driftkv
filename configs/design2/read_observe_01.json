{
  "reason": "Source-only2048 reduces primary severe rate but leaves the >1 residual and insufficient excess-mass recovery; test the already calibrated query-aware nested observation",
  "score": "source plus actual native response,225 coordinates; use already frozen evidence_01 observed_bound coefficients and90% threshold unchanged",
  "transfer_boundary": "Calibration uses16-query panel maxima; runtime uses actual first request candidates. This distribution change is evaluated, not called decision preservation.",
  "execution": "capture ordinary Design1 first read; evaluate225D observed bound; if screened reconstruct Current KV, compare actual logits using unchanged0.0430952204 tolerance; chosen logit reused and installed KV persists",
  "population": "8UID canary;512 natural then2048 natural and512 short32/128 if computation/numerics viable; no outcome-based threshold or group change",
  "comparison": "source-only gate and group-background gate with the SAME paid reconstruction/commit action, and original full-check source candidate; complete preparation/reading/rejected-build cost",
  "scope": "development; cannot certify future queries or solve same-observation aliasing"
}
