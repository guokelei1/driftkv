# Model primitives

This package keeps the reusable HSTU architecture, attention blocks, embeddings, RMSNorm, and persistent K/V cache. Experimental gauges and model variants from retired experiments were removed.
