# Data primitives

This package currently keeps only the raw KuaiRand loader and a chronological streaming plan. Dataset-specific corpus builders and prepared-artifact readers from retired experiments were removed.
