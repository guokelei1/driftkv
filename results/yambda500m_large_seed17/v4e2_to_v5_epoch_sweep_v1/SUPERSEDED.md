# Endpoint sweep status

该目录是保留的 post-hoc direct-lineage development evidence。当前 canonical V5 只使用
`checkpoints/checkpoint_epoch_2.pt`；V5@1.0 checkpoint 与失败门结果不是当前模型，但保留用于审计
一 epoch 与两 epoch 的完整比较。

当前序列入口为 `../canonical_D14_v0_v5_v1/chain.json`。
