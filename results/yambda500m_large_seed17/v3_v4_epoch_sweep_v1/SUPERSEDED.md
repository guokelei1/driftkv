# Endpoint sweep status

该目录是保留的 post-hoc endpoint development evidence。当前 canonical V4 只使用
`checkpoints/checkpoint_epoch_2.pt`；0.5/1.0/1.5 epoch checkpoint、对应 raw score、seal、
adjudication 和失败结果均不是当前模型，但保留用于审计为何选择两 epoch。

当前序列入口为 `../canonical_D14_v0_v5_v1/chain.json`。
