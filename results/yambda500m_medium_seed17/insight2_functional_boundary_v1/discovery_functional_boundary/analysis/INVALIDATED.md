# Invalidated adjudication view

This first analysis view is retained for provenance but must not be cited.
It promoted gap-weighted recovery to the primary metric, whereas the frozen
protocol defines the unclipped per-user recovery followed by edge-equal
averaging. The raw parquet files are valid and unchanged. The corrected view
is `../analysis_v2/` and reports the gap-weighted quantity only as sensitivity.
